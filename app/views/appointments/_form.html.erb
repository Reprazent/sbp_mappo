<%= form_for([current_user, @appointment.patient, @appointment]) do |f| %>
<% if @appointment.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

	<ul>
		<% @appointment.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>
<p>
	<%= f.label :title%>
	<%= f.text_field :title %>
</p>
<p>
	<%= f.label :date%>
	<%= f.text_field :date %>
</p>
<p>
	<%= f.label :start_time, :include_blank => true%>
	<%= f.time_select :start_time, :include_blank => true %>
</p>
<p>
	<%= f.label :end_time, :include_blank => true%>
	<%= f.time_select :end_time, :include_blank => true %>
</p>
<div class="actions">
	<%= f.submit %>
</div>
<% end %>
