<%= form_for([current_user, @appointment.patient, @appointment]) do |f| %>
<% if @appointment.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

	<ul>
		<% @appointment.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>
<p>
	<%= f.label :title%>
	<%= f.text_field :title %>
</p>sche
<p>
	<%= f.label :date%>
	<%= f.date_select :date %>
</p>
<p>
	<%= f.label :start_time %>
	<%= f.time_text_field :start_time %>
</p>
<p>
	<%= f.label :end_time%>
	<%= f.time_text_field :end_time %>
</p>
<div class="actions">
	<%= f.submit %>
</div>
<% end %>
